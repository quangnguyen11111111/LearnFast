import type { Question } from "./types";


export default function useCardMatching(initialData: Question[]) {
  // Logic cho trò chơi ghép thẻ sẽ được triển khai ở đây
  return {
    // Trả về các trạng thái và hàm xử lý cần thiết
    
  }
}